import React from 'react';

import useStyles from "./style";

const NoMatch = () => {
    const classes = useStyles();

    return (
        <div>
            NoMatch
        </div>
    );
};

export default NoMatch;