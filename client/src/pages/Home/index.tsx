import React from 'react';

import useStyles from "./style";

const Home = () => {
    const classes = useStyles();

    return (
        <div>
            Home
        </div>
    );
};

export default Home;