import React from 'react';

import useStyles from "./style";

const Orders = () => {
    const classes = useStyles();

    return (
        <div>
            Orders
        </div>
    );
};

export default Orders;